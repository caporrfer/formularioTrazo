server {
    listen 80;

    location /api/ {
        proxy_pass http://rpi-metrics-api:5000/;
        proxy_read_timeout 10s;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
